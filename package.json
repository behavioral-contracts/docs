{
  "name": "@behavioral-contracts/docs",
  "version": "1.0.0",
  "private": true,
  "description": "Documentation for Behavioral Contracts",
  "scripts": {
    "docs:generate": "npm run docs:generate-cli && npm run docs:generate-schema && npm run docs:generate-packages",
    "docs:generate-cli": "ts-node scripts/generate-cli-docs.ts",
    "docs:generate-schema": "ts-node scripts/generate-schema-docs.ts",
    "docs:generate-packages": "ts-node scripts/generate-package-docs.ts",
    "docs:check-stale": "ts-node scripts/check-stale.ts",
    "validate": "npm run validate-examples",
    "validate-examples": "ts-node scripts/validate-examples.ts",
    "docusaurus": "docusaurus",
    "prestart": "npm run docs:check-stale",
    "start": "docusaurus start",
    "prebuild": "npm run docs:check-stale",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@docusaurus/core": "^3.5.2",
    "@docusaurus/preset-classic": "^3.5.2",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.5.2",
    "@docusaurus/tsconfig": "^3.5.2",
    "@docusaurus/types": "^3.5.2",
    "@types/node": "^20.0.0",
    "@types/js-yaml": "^4.0.5",
    "glob": "^10.0.0",
    "js-yaml": "^4.1.0",
    "ts-node": "^10.9.0",
    "typescript": "~5.2.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0"
  }
}
